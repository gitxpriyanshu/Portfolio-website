# version removed

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: priyanshu-portfolio
    ports:
      - "3000:80"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
